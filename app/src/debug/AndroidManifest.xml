<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.powerai">

    <application>
        <!-- In debug builds we keep EmbeddingTestActivity available for manual launch,
             but do NOT override the app launcher intent so MainActivity remains the entrypoint. -->
        <activity
            android:name=".ui.test.EmbeddingTestActivity"
            android:exported="false"
            tools:replace="android:exported" />
    </application>

</manifest>
