package com.example.powerai.ui.screen.hybrid

/**
 * Hybrid 模块的一次性副作用
 * 目前预留扩展，后续可添加导航、Toast 等
 */
sealed interface HybridEffect {
    // 预留：导航到详情页等
    // data class NavigateToDetail(val id: Long) : HybridEffect
}
