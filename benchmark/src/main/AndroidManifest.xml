<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <receiver
            android:name="androidx.benchmark.macro.ProfileInstallReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.DUMP">
            <intent-filter>
                <action android:name="androidx.benchmark.macro.action.INSTALL_PROFILE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
